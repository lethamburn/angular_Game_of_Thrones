import { DecodehtmlstringPipe } from './decodehtmlstring.pipe';

describe('DecodehtmlstringPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodehtmlstringPipe();
    expect(pipe).toBeTruthy();
  });
});
